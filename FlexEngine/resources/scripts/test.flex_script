func pow(int val, int depth) -> int {
  if (depth > 0) {
    return val * pow(val, depth - 1);
  }
  return val;
}

out_0 = pow(2, 8);
Print(out_0);

out_1 = DoMathSmall(1, 2, 3);

out_2 = Quintuple(2);

Print(false);
Print(1000);
Print(100.50);
Print("Hello, world!");

